<% if model.errors.any? %>
  <div class="alert alert-warning">
  	<h3><%= pluralize(model.errors.count, "error") %> prohibited this model from being saved:</h3>
    <ul>
      <% model.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>